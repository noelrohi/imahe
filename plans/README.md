# Implementation Plans

Generated by the improve skill on 2026-06-28 for **imahe** — an Electron desktop wrapper around [ima2-gen](https://github.com/lidge-jun/ima2-gen). Design rationale lives in `CONTEXT.md` and `docs/adr/0001`–`0003`; read those terms before executing (each plan also inlines the parts it needs).

This is a **greenfield build-out**, not an audit. Plans turn the agreed design into executable steps. Execute in the order below unless dependencies say otherwise. Use **Bun** for dependency/script commands (not npm), read the plan fully before starting, honor its STOP conditions, and update your row when done.

> **No git baseline yet.** At authoring time the repo had zero commits (the scaffold was uncommitted). Plan 001 creates the first commit. Plans 002 and 004 drift-check against that commit; plan 003 drift-checks against plan 002. Record the relevant SHA as each dependency lands — until then, compare against the working tree.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Project foundations: React + Tailwind + shadcn, verification baseline, baseline commit | P1 | M | — | DONE (2ada510) |
| 002  | Sidecar lifecycle: bundle & spawn ima2, port + health, preload bridge | P1 | M | 001 | DONE (c1550df) |
| 003  | imahe SQLite store: schema, migrations, asar.unpack, IPC data layer | P1 | M | 001, 002 | DONE (cc0931b) |
| 004  | App shell + shadcn sidebar navigation | P1 | S | 001 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

## Dependency notes

- **001 unblocks everything** — it introduces React/Tailwind/shadcn and the typecheck + test commands every later plan's verification gates rely on.
- **002 should land before 003**. The sidecar and store are conceptually separate, but both touch `package.json`, `forge.config.ts`, `src/main.ts`, `src/preload.ts`, `src/shared/ipc.ts`, and `src/global.d.ts`; serialize them to avoid merge-conflict-prone integration work.
- **004 depends only on 001** but touches `package.json`/`bun.lock` for the router. It can run after 001 on a separate branch, but rebase before merging if 002/003 changed dependencies.
- Feature plans (005 auth, 006 gallery, 007 generate/variants, 008 remix/lineage, 009 canvas, 010 collections) are **not yet written** — they will be authored after 001–004 land, so they can cite real code patterns instead of guesses. See `CONTEXT.md` → "UI shape" and "Scope decisions" for their intended shape.

## Findings considered and rejected

- **Use ima2's CLI instead of the HTTP API**: rejected — ima2's `gen`/`edit`/`multimode`/`ls` commands themselves require a running `ima2 serve` (they are thin HTTP clients), so the CLI adds a process spawn + stdout parsing on top of the same server lifecycle. imahe calls the HTTP API directly. (See `CONTEXT.md` flagged ambiguities.)
- **Embed ima2's web UI in a BrowserWindow**: rejected in ADR 0001 — defeats the custom gallery/remix/variants UI that is the point of imahe.

## Open verification tasks (carry into later plans)

- **Favorites ownership** — `ima2 ls --favorites` suggests ima2 may track favorites server-side. Verify against ima2's `docs/API.md` or a live server before plan 010; if confirmed, the imahe store owns collections + lineage only, not favorites.
- **Exact request/response field shapes** per ima2 endpoint — verify against `docs/API.md` or a live `ima2 serve` when wiring each call (plans 005–010). Endpoint *paths* in this repo's docs are reliable; field names are not yet confirmed.
